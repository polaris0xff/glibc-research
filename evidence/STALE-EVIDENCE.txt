# evidence/STALE-EVIDENCE.txt — evidence known to describe an older instrument
#
# ⛔ THIS FILE IS A DEBT LEDGER, NOT AN EXCUSE. Every line names an experiment
# whose committed evidence was written BEFORE the last non-comment change to
# the script that produces it. `scripts/common/check-docs.sh` gate 10 fails on
# such a pair unless it is listed here, so what is in this file is visible,
# counted, and owned by an entry.
#
# ⚠ AN ENTRY IS PINNED TO BOTH COMMITS AND STOPS MATCHING WHEN EITHER MOVES.
# Re-run the experiment and the pair changes, the line goes inert and should be
# deleted. Change the script again and the pair changes, the gate fires again.
# A line cannot silently outlive its reason.
#
# Format:  <script basename> <script-commit> <evidence-commit> <reason>
#
# ---------------------------------------------------------------------------
# ⛔ ALL FOUR BELOW ARE THE SAME DEBT, and it is T-070's residue.
#
# Found by deep review 6 on 2026-09-03c. Each of these scripts used to carry
#
#     ENV_ROOT="$ROOTFS_DIR/${PGB_ENV_NAME:-pgb-env-debian12}"
#
# and now takes the name from `internal/cfg/cfg.go`, which says
# `pgb-env-debian13`. So the committed numbers were measured inside
# **glibc 2.36** and the script now measures **glibc 2.41**. That is not a
# cosmetic diff: it is the build environment the whole experiment runs in.
#
# ⚠ T-070 measured the pin move and found its four named costs at zero, so the
# numbers may well be unchanged — but nobody has re-run these, and until
# somebody does the record must not imply otherwise. **T-077 owns it.**
#
# ⛔ 60-, 61- and 62- are THE HEAD-TO-HEAD. `docs/comparison.md` and
# `docs/REQUIREMENTS.md`'s table — artefact size, malloc throughput, the
# eleven-row coverage against AppImage/Flatpak/snap/onelf/static-musl — all
# come from them. They are pinned here rather than re-run because between them
# they build five delivery formats and run two benchmark matrices; that is
# hours, not minutes, and it needs the machine to itself for the clock rows.
#
61-libc-throughput.sh 8ce829252d942b05a25a484b780148d427b1890e 0e56e62962e7d461dca7b57ebc9534476b0a4941 T-077: measured on pgb-env-debian12/glibc-2.36; benchmark matrix, needs an idle machine for the clock
88-nonix-end-to-end.sh 8ce829252d942b05a25a484b780148d427b1890e 25a86348fced4718085f9ef5997e103f7de82299 T-077: measured on pgb-env-debian12/glibc-2.36; five arms, plans and builds jq inside a rootfs

# ---------------------------------------------------------------------------
# ⛔ SIX MORE, ADDED 2026-09-04c, AND THEY ARE ONE CHANGE: T-084 STEP 2.
#
# Every one of these six carried its own hand copy of the trace classifier.
# The copies are deleted and each script now calls `experiments/lib.sh`'s
# `exp_classify_trace`, so their committed host counts were produced by an
# implementation that is no longer in the tree. ⛔ That is a real debt and it
# is exactly what this ledger is for.
#
# ⭐ THE DIRECTION AND THE REACH ARE MEASURED, NOT GUESSED — `experiments/102-`,
# which runs the six copies back out of git against the shared classifier on
# five fixtures:
#
#   C25  all six counted a SPLIT failed open as a load.   CLEAN → DIRTY
#   C38  five cleared their set on the artefact's own execve unconditionally,
#        so in `tree` mode a row read clean.              DIRTY → CLEAN
#        `60-` never cleared there and errs the other way, in `payload` mode.
#
# ⛔ AND C38 REACHES EXACTLY ONE OF THEM — `90-` — because it is the only one
# that calls the mode its copy differed in AND invokes the artefact more than
# once in its traced run (102- arm S). ⚠ The other five are LATENT, which
# means their numbers are expected to stand, not that anyone has shown they do.
#
# ⛔ 60-, 61- and 62- are THE HEAD-TO-HEAD, and `docs/comparison.md` plus
# `docs/REQUIREMENTS.md`'s table come from them. 60- and 62- also still carry
# T-077's debt above: they were measured on pgb-env-debian12/glibc-2.36.
#
60-versus-alternatives.sh 775172d72fa22a7d243eadb71882e874485d4859 9cb85154035f2a81824ca8428cb4419c44567d0d T-077 and T-084 step 2: glibc-2.36 evidence AND the classifier converted; builds five delivery formats, needs an idle machine
62-anylinux-appimage.sh 775172d72fa22a7d243eadb71882e874485d4859 894bfaec26de04327d143a913f4a39bafbd567d8 T-077 and T-084 step 2: glibc-2.36 evidence AND the classifier converted; builds an anylinux AppImage on an Arch bed
85-opengl.sh 775172d72fa22a7d243eadb71882e874485d4859 d79949c4e77f523c04528e7f37e4e4f1d250ccae T-084 step 2: classifier converted; calls payload only and invokes the artefact once, so C38 is latent (102- arm S)
86-bundler-vs-anylinux.sh 775172d72fa22a7d243eadb71882e874485d4859 403a0943a396734f821626e51b5580225e18048b T-084 step 2: classifier converted; calls payload only, so C38 is latent (102- arm S); builds kdenlive-scale bundles
89-debloat.sh 775172d72fa22a7d243eadb71882e874485d4859 44b6a1c89fe011e4988acfcc5ffc9189afac5bbe T-084 step 2: classifier converted; calls payload only and invokes the artefact once, so C38 is latent (102- arm S)
# ⭐ 90- WAS HERE AND IS GONE, 2026-09-04c. It was the ONE experiment C38
# reached, so it was the one the conversion actually owed a re-run — and the
# re-run happened: ours 11/11 clean, the competitor 4/11, both column totals
# unchanged and every per-row count now describing BOTH invocations.

# ⭐ AND ONE LINE WAS DELETED HERE, 2026-09-04c, WHICH IS THE POINT OF THE
# FORMAT. `65-capability-corpus.sh` was pinned as "NOT A DEBT: the experiment
# has never been run". It has now run to completion — 26 of 26 rows — so the
# pair moved, the line went inert, and it is gone rather than left to be read
# as a current statement about the corpus.

# ⛔ ONE MORE, 2026-09-04c, AND IT IS THE SHORTEST-LIVED ENTRY HERE.
# `experiments/103-` ran once (its RESULT.txt is that run) and then had BOTH
# of run 1's own defects fixed: the decode leg used `wavenc`, which lives in
# gst-plugins-good and is not in the closure, and the control could not be
# told from its subject. ⭐ Run 2 is what removes this line; it needs the bed,
# which the corpus was holding when the fix landed.
103-gstreamer-decode.sh ce91364c817cbee71494c75f573f6977232b1d9f 82a66bf6c2746701d9c3a0614dccd1cb134364fc T-091: run 1 is committed; run 2 is owed after the two criterion fixes it exposed

# ⛔ C49, 2026-09-04c — AND THIS ONE IS A COMMENT AND NOT A PINNED LINE, ON
# PURPOSE. The classifier that produces the host-object count for EVERY
# experiment lives in `experiments/lib.sh`, not in any one script, so gate 10 —
# which compares a script's own commit against its evidence's — cannot see the
# change and no pinned pair would ever go inert.
#
# `exp_classify_trace`'s host test was a prefix list with `bundled` as its
# complement, so a host object outside `/lib`, `/usr/lib` and friends was
# scored as the artefact's own. Measured across all eleven rootfs, thirteen
# files fall outside it, and one of them is `/usr/bin/ld.so` — THE HOST
# LOADER, on arch, fedora-42, debian-12 and debian-13.
#
# ⚠ SO EVERY COMMITTED HOST-OBJECT COUNT IN THE TREE PREDATES THE FIX. The
# argument that none of them moves is stated in docs/history/corrections.md
# C49 and it is an ARGUMENT, not a measurement: all thirteen files are
# reachable only by running `stdbuf`, `sudo`, or the host loader by that path,
# and no subject in the corpus does any of those.
#
# ⭐ WHAT REMOVES THIS BLOCK: a `65-` corpus re-run under the fixed classifier.
# Until then a zero in this tree means "clean under the OLD predicate", and
# anyone quoting one should say so.
