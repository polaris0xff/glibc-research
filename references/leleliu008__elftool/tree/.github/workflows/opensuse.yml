name: openSUSE

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  opensuse:
    strategy:
      fail-fast: false
      matrix:
        flavor: ['tumbleweed', 'leap:15.4']

    runs-on: ubuntu-latest

    container: opensuse/${{ matrix.flavor }}

    steps:
      - run: zypper update  -y
      - run: zypper install -y curl tar gzip

      - uses: actions/checkout@main

      - run:  ./build.sh

      - run:  ./elftool

      - run:  ./elftool --help
      - run:  ./elftool --version

      - run:  ./elftool -h
      - run:  ./elftool -V

      - run:  ./elftool print-interp elftool
      - run:  ./elftool print-soname elftool
      - run:  ./elftool print-needed elftool
      - run:  ./elftool print-rpath  elftool
